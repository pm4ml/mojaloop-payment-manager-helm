apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mojaloop-finflux-core-connector.fullname" . }}-test-connection"
  labels:
{{ include "mojaloop-finflux-core-connector.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mojaloop-finflux-core-connector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
