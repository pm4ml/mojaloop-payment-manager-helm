apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mojaloop-ooredoo-core-connector.fullname" . }}-test-connection"
  labels:
{{ include "mojaloop-ooredoo-core-connector.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mojaloop-ooredoo-core-connector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
