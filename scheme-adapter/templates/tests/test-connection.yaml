apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scheme-adapter.fullname" . }}-test-connection"
  labels:
{{ include "scheme-adapter.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "scheme-adapter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
